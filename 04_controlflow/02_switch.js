// switch (key) {
//     case value:
        
//         break;

//     default:
//         break;
// }
const month=3

switch (month) {
    case 1:          //here write like if else in conditin write var which we have to check
        console.log("Jan");                      //in case we write comparison
        break;
    case 2:          //shortcut key for duplicate shift+alt hold them and then press downward arrow
        console.log("Feb");
        break;
              
    default:
        console.log("March");  //if we don't write break statement then it prints all except default
        break;
}

const mon="Feb"

switch (mon) {
    case "Jan":          //here write like if else in conditin write var which we have to check
        console.log("Jan");                      //in case we write comparison
        break;
    case "Feb":          //shortcut key for duplicate shift+alt hold them and then press downward arrow
        console.log("Feb");
        break;
              
    default:
        console.log("March");  //if we don't write break statement then it prints all except default
        break;
}